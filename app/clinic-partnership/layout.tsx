import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Clinic Partnership — Fertility Lifestyle Companion for Your Patients",
  description: "Offer your patients an evidence-based fertility optimization app. Cycle-synced exercises, nutrition, male fertility program, and couple mode. Reduce counseling calls by 60%.",
};

export default function ClinicPartnershipLayout({ children }: { children: React.ReactNode }) {
  return children;
}
