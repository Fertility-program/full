import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Community — Connect with Other Couples TTC",
  description: "Join our fertility community on Telegram, Facebook, and Instagram. Share experiences, get support, and celebrate wins together.",
};

export default function CommunityLayout({ children }: { children: React.ReactNode }) {
  return children;
}
